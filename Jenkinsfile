pipeline {

    agent {
//       node { label 'workstation'}
        label 'JAVA'
    }

    stages {

        stage('One') {
            steps {
                sh 'echo Hello'
            }
        }
    }
}